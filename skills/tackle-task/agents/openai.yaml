interface:
  display_name: "Tackle Task"
  short_description: "Investigate an issue before deciding whether to ship"
  default_prompt: "Use $tackle-task to investigate this issue against the current code before recommending whether to ship it."
