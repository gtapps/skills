Adversarially redesign this plan before implementation. Treat the proposal as
one candidate, not the presumed solution. Verify the diagnosis, find the
deepest appropriate fix, challenge model-driven or duplicated orchestration,
and prefer deterministic ownership at the layer that owns the resulting data.

Return:

1. Whether the premise is correct
2. Problems with the proposed approach
3. The best solution you can design
4. Proposed vs. recommended trade-offs
5. Exact files and regression tests
6. Final verdict

Ground everything in the live repository: read the actual code before asserting
anything about it, and cite what you read as `path/to/file.ext:line`. Where the
plan's premise depends on behavior you cannot confirm in the repository, say so
explicitly instead of assuming it holds. Do not make changes.

--- PLAN UNDER REVIEW ---
